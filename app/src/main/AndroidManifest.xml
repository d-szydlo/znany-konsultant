<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.znanykonultant">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ZnanyKonultant">
        <activity android:name=".consultant.profile.ConsultantServiceEdit"></activity>
        <activity android:name=".consultant.profile.ConsultantPage" />
        <activity android:name=".consultant.ConsultantMainPageActivity"></activity>
        <activity android:name=".login.ForgotPasswordActivity"></activity>
        <activity android:name=".consultant.ConsultantServiceEdit" />
        <activity android:name=".consultant.ConsultantPage" />
        <activity android:name=".consultant.ConsultantMainPageActivity" />
        <activity android:name=".user.UserMainPageActivity" />
        <activity android:name=".registration.RegistrationActivity" />
        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>